import { Meta, StoryObj } from '@storybook/react'

import SubmitProjectForm from '.'

export default {
  title: 'SubmitProjectForm',
  component: SubmitProjectForm
} as Meta

export const Default: StoryObj = {}
